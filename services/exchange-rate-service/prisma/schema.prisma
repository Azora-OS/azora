generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Exchangerateservice {
  id        String   @id @default(cuid())
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  
  @@map("exchange_rate_service")
}