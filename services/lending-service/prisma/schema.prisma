generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Lendingservice {
  id        String   @id @default(cuid())
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  
  @@map("lending_service")
}