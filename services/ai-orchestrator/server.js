const app = require('./index.js');

// Simple console log to indicate service startup
console.log('AI Orchestrator Service starting...');

// The Express app is already configured and listening in index.js
// This file just serves as the entry point