generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Tamperproofdataservice {
  id        String   @id @default(cuid())
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  
  @@map("tamper_proof_data_service")
}