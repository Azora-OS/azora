generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Azorajudiciaryservice {
  id        String   @id @default(cuid())
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  
  @@map("azora_judiciary_service")
}