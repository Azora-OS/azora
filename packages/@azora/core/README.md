# @azora/core (placeholder)

This is a lightweight placeholder package for `@azora/core` to satisfy workspace resolution. Implementations should replace this placeholder with the project's core utilities and types.

Quick commands:

```powershell
pnpm --filter @azora/core run build
```
