# Replace the following tokens in git history with REPLACED to prevent leaks

# Exact string to replace
DATABASE_URL==REDACTED_DATABASE_URL
SUPABASE_ANON_KEY==REDACTED_SUPABASE_ANON_KEY
JWT_SECRET==REDACTED_JWT_SECRET
SUPABASE_URL==REDACTED_SUPABASE_URL

# Replace common AWS ID patterns (regex style is supported by git-filter-repo)
regex:AKIA[0-9A-Z]{16}
==REDACTED_AWS_KEY

# Stripe keys
regex:sk_live_[A-Za-z0-9]{24,}
==REDACTED_STRIPE_KEY
regex:sk_test_[A-Za-z0-9]{24,}
==REDACTED_STRIPE_TEST_KEY

# Slack tokens
regex:xox[baprs]-[A-Za-z0-9-_.]+
==REDACTED_SLACK_TOKEN

# Other sensitive patterns
SUPABASE_SERVICE_ROLE_KEY==REDACTED_SUPABASE_SERVICE_ROLE_KEY
X_API_KEY==REDACTED
PASSWORD==REDACTED
