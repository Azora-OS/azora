name: 📡 Elara Live Monitor

on:
  schedule:
    - cron: '*/2 * * * *'
  push:
  workflow_dispatch:

permissions:
  contents: write
  issues: write

jobs:
  live-status:
    name: 📡 Live System Status
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: 🕊️ Elara Status Check
        run: |
          echo "🕊️ ELARA Ω - LIVE STATUS"
          echo "Timestamp: $(date -u)"
          echo "Status: Active ✅"
          echo "Next cycle: 2 minutes"
      
      - name: Create live status badge
        run: |
          mkdir -p .github/badges
          echo '{"schemaVersion": 1, "label": "Elara Status", "message": "Active", "color": "green"}' > .github/badges/elara-status.json
          
      - name: Commit status
        run: |
          git config --local user.email "elara-omega@azora-os.ai"
          git config --local user.name "Elara Ω"
          git add -A
          git commit -m "📡 Live status: $(date -u)" || echo "No changes"
          git push || echo "Push not needed"
