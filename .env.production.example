# Production JWT Secrets
# Generated: 2025-12-04T05:51:00+02:00
# IMPORTANT: Copy these to your .env file (NOT .env.example)

# Generate your own secrets with:
# node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"

JWT_SECRET=GENERATE_YOUR_OWN_SECRET_128_CHARS
JWT_REFRESH_SECRET=GENERATE_YOUR_OWN_DIFFERENT_SECRET_128_CHARS

# Instructions:
# 1. Run the command above twice to generate two different secrets
# 2. Copy the output to your .env file
# 3. Never commit .env to git
# 4. Keep these secrets secure and rotate them every 90 days
